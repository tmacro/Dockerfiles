name: Publish Images
description: Publish built images based on definitions
inputs:
  changes:
    description: images with file changes
  updates:
    description: images with updated dependencies
  docker_username:
    description: Docker username
  docker_token:
    description: DockerHub token
runs:
  using: 'node12'
  main: 'index.js'